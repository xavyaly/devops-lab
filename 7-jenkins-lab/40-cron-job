pipeline {
    agent any
    options { 
        buildDiscarder(logRotator(numToKeepStr: '5')) 
        timestamps()
    }
    triggers{
        cron('* * * * *')
    }
    stages {
        stage("cron") {
            steps {
                echo "Execute after each minute"
            }
        }
    }
}

